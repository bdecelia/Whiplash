<head>
  <script src="https://code.highcharts.com/modules/exporting.js"></script>
</head>
<body>
  <div class="graph-box" style="margin-top: 200x">
    <div id="word-container" style="min-width: 310px; height: 400px; margin: 0 auto"></div>
  </div>

  <script type="text/javascript">

  $(document).ready(function () {

    //retrieve data
    $.ajax({
       url: 'https://jsonplaceholder.typicode.com/comments',
       success: function(data) {
         var word_data = []
         var count_data = []
         for(var i = 0; i < 100; i++){
           word_data.push(data[i].name)
           count_data.push(data[i].id)
         }
           my_highchart(word_data, count_data )
       }
   });

   //highchart column graph
   function my_highchart(word_data, count_data){
    let chart =   Highcharts.chart('word-container', {
          chart: {
              type: 'column'
          },
          title: {
              text: 'Hot Words'
          },
          xAxis: {
              categories: [],
              crosshair: true
          },
          yAxis: {
              min: 0,
              title: {
                  text: 'Word count'
              }
          },
          tooltip: {
              headerFormat: '<span style="font-size:10px">{point.key}</span><table>',
              pointFormat: '<tr><td style="color:{series.color};padding:0">{series.name}: </td>' +
                  '<td style="padding:0"><b>Used {point.y:.1f} times</b></td></tr>',
              footerFormat: '</table>',
              shared: true,
              useHTML: true
          },
          plotOptions: {
              column: {
                  pointPadding: 0.2,
                  borderWidth: 0
              }
          },
          series: [{
              name: 'Hot Words',
              data: []
          }]
      });

      //set chart data
      chart.xAxis[0].setCategories(word_data)
      chart.series[0].setData(count_data)
    }
});
  </script>
</body>
